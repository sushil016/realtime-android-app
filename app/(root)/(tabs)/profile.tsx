import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Profile() {
  return (
    <SafeAreaView>
      <Text className="text-3xl bg-black text-white px-4 py-2">profile hain je</Text>
    </SafeAreaView>
  );
}